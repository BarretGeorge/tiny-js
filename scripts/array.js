let arr = [1, 2, 3];

arr.push(4, 5);

println(arr);

println(arr.length);

println(arr.join(","));


let str = "Hello, World!";

println(str.length);

for (let i = 0; i < str.length; i++) {
    println("i=", i);
    println(str.at(i));
}
